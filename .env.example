# File: meta-tabletop/.env.example
# Project: CLIENT
# Purpose: Example environment variables for local development

# API URL for development (should match your Worker's dev server)
PUBLIC_API_URL=http://localhost:8787

# Other environment variables as needed
# PUBLIC_* variables are accessible in client-side code